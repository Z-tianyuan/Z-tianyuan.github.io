---
title: NucleiAI 项目完整学习指南 — 面试准备
date: 2026-05-27T22:00:00+08:00
draft: false
categories:
  - 项目实战
tags:
  - NucleiAI
  - 面试准备
  - 学习笔记
---

# NucleiAI 项目完整学习指南

> 读完这份指南，你能自信地回答关于这个项目的任何面试问题。

## 目录

1. [项目鸟瞰：问题、方案、结果](#1-项目鸟瞰)
2. [完整数据流：一次扫描的生命周期](#2-数据流)
3. [文件逐行精讲](#3-文件精讲)
4. [核心技术决策](#4-技术决策)
5. [Nuclei 模板 DSL 速查](#5-模板dsl)
6. [面试叙事框架](#6-面试叙事)
7. [常见追问清单](#7-追问清单)

---

## 1. 项目鸟瞰

### 一句话

> 在 Nuclei 扫描引擎之上集成 Ollama 本地 LLM，用"AI 语义分析 + 硬规则修正"的混合防线自动过滤误报，通过 Web 面板展示结果并生成中文安全报告。

### 核心数据

| 指标     |           数值           |
| ------ | :--------------------: |
| 代码量    |        ~2800 行         |
| 文件数    |          39 个          |
| 自定义模板  | 20 个（5 技法演示 + 15 漏洞检测） |
| 靶场端点   | 20 个（14 真漏洞 + 6 误报干扰）  |
| AI 准确率 |   最终轮 100%（稳态 90%+）    |
| 扫描耗时   |        ~3-5 分钟         |

### 技术栈（面试版）

```
扫描引擎: Nuclei v3.8.0 (Go 编写，YAML 驱动)
指纹识别: httpx (Go 编写)
AI 推理: Ollama + qwen3:8b (Q4_K_M 量化)
后端框架: FastAPI (Python 异步)
前端模板: Jinja2
前端样式: 原生 CSS（深色主题，无框架依赖）
靶场: Python http.server（标准库，零依赖）
```

### 为什么选这些技术

- **Nuclei**: 社区最活跃的开源漏洞扫描器，YAML 模板生态丰富，3000+ 官方模板
- **Ollama**: 本地部署 LLM 的最简方案，一行命令拉模型，HTTP API 调用，零费用
- **qwen3:8b**: 中文理解能力好，8B 参数在消费级 GPU/CPU 上可运行
- **FastAPI**: Python 异步框架，自带 Swagger 文档，适合 API + 模板渲染
- **纯 CSS**: 不引入前端框架依赖，减少项目复杂度，面试展示原生能力

---

## 2. 数据流

### 一次完整扫描的数据流

```
用户在浏览器输入 http://127.0.0.1:9999
         │
    ┌────▼────┐
    │  POST / │  do_scan()
    └────┬────┘
         │
    ┌────▼────────────┐
    │ Step 1: 指纹识别  │  fingerprint_target() → httpx
    │ 输出: {           │
    │   technologies,   │
    │   web_server,     │
    │   status_code     │
    │ }                 │
    └────┬────────────┘
         │
    ┌────▼────────────┐
    │ 智能模板匹配      │  suggest_templates(technologies)
    │ "Apache+PHP" →   │  → 匹配 TECH_TEMPLATE_MAP
    │   2个官方模板     │  → 返回具体路径列表
    └────┬────────────┘
         │
    ┌────▼────────────┐
    │ Step 2: 漏洞扫描  │  run_nuclei_scan()
    │ nuclei -t        │  → subprocess 调用 nuclei
    │   custom-templates│  → 输出 JSONL 文件
    │   + 智能模板      │  → 解析得到 list[dict]
    │ 输出: 26条原始结果 │
    └────┬────────────┘
         │
    ┌────▼────────────┐
    │ Step 3: AI 过滤   │  filter_results()
    │ ┌──────────────┐ │  → 每 5 条一个 batch
    │ │ _summarize() │ │  → 构建 Prompt
    │ └──────┬───────┘ │  → 调用 Ollama API
    │ ┌──────▼───────┐ │  → 解析 JSON 数组
    │ │ 5条硬规则修正 │ │  → 技术识别保护
    │ │ XSS转义检测   │ │  → XSS 转义检测
    │ │ 内部重定向    │ │  → 内部重定向
    │ │ 调试已关闭    │ │  → 调试已关闭
    │ │ Git文字提及   │ │  → Git 文字提及
    │ └──────────────┘ │
    │ 输出: confirmed[] │
    │       false_pos[] │
    └────┬────────────┘
         │
    ┌────▼────────────┐
    │ Step 4: 渲染展示  │
    │ scan_result_to_row│  → 提取展示字段
    │ → index.html     │  → 仪表盘表格
    │ → report.html    │  → PDF 报告
    │ → compare.html   │  → Diff 对比
    └─────────────────┘
```

### 核心数据结构

```python
# Nuclei 原始输出（JSONL 每行）
{
    "template-id": "vuln-xss-reflected",
    "info": {
        "name": "Reflected XSS via Search Parameter",
        "severity": "medium",
        "tags": ["vuln", "xss", "reflected"],
        "description": "搜索参数 q 的值被原样回显..."
    },
    "url": "http://127.0.0.1:9999/search?q=<script>...",
    "matched-at": "http://127.0.0.1:9999/search?q=...",
    "request": "GET /search?q=... HTTP/1.1\r\n...",
    "response": "HTTP/1.0 200 OK\r\n...\r\n\r\n<html>...",
}

# AI 判定后追加的字段
{
    "ai_verdict": {
        "finding_type": "vuln",        # "vuln" | "tech" | "exposure"
        "is_false_positive": False,
        "confidence": 0.95,
        "reason": "用户输入被原样回显，存在XSS执行点"
    }
}

# 前端展示用的扁平结构
{
    "name": "Reflected XSS via Search Parameter",
    "severity": "medium",
    "description": "搜索参数 q 的值被原样回显...",
    "tags": "vuln, xss, reflected",
    "url": "http://127.0.0.1:9999/search?q=...",
    "finding_type": "vuln",
    "is_false_positive": False,
    "confidence": 0.95,
    "reason": "用户输入被原样回显..."
}
```

---

## 3. 文件精讲

### 3.1 `web/app.py` — 主应用（243 行）

**职责**: FastAPI 路由 + 扫描编排 + 全局状态管理

```
路由表:
  GET  /              → index()       仪表盘首页
  POST /              → do_scan()     提交扫描任务
  GET  /report/{id}   → view_report() 查看报告
  GET  /compare       → compare_view() 扫描 Diff
```

**关键函数**:

```python
# 扫描编排（do_scan 的核心流程）
result = {
    "target": target,
    "fingerprint": None,      # httpx 指纹结果
    "smart_templates": [],    # 智能匹配的官方模板
    "_raw_results": [],       # Nuclei 原始输出
    "findings": [],           # 前端展示用
    "confirmed": [],          # AI 确认的真实漏洞
    "false_positives": [],    # AI 判定的误报
    "ai_enabled": True,       # AI 是否可用
}
```

```python
# 扫描结果对比键
def _finding_key(r: dict) -> str:
    # 用 "模板名@URL" 作为唯一标识
    # 同一个漏洞在两次扫描中应该产生相同的 key
    tid = info.get("name", r.get("template-id", "?"))
    url = r.get("url") or r.get("matched-at") or r.get("host", "") or "?"
    return f"{tid}@{url}"
```

```python
# Diff 对比逻辑
def compare_scans(scan_a, scan_b):
    # 核心思路：集合运算
    keys_a = {_finding_key(r): r for r in raw_a}  # 扫描A的key集合
    keys_b = {_finding_key(r): r for r in raw_b}  # 扫描B的key集合

    # in A not in B → 已修复
    # in B not in A → 新增
    # in both       → 持续存在
```

**面试重点**: `SCAN_HISTORY` 是全局变量（内存中），重启就丢失。这是因为项目定位是演示/本地使用，生产环境应该用数据库。

---

### 3.2 `core/ai_filter.py` — AI 过滤引擎（221 行）

**职责**: 批量调用 LLM + 硬规则修正，是整个项目的核心模块

**核心流程**:

```python
async def filter_results(results, ...):
    # 1. 分批次（BATCH_SIZE=5，平衡速度和准确性）
    for batch_start in range(0, len(results), BATCH_SIZE):
        batch = results[batch_start:batch_start + 5]
        verdicts = await _process_batch(batch, ...)

    # 2. 对每条结果应用 5 条硬规则修正
    for result, verdict in zip(results, verdicts):
        # 规则 1: tech 类型永不标误报
        # 规则 2: HTML 转义过的 XSS → 自动误报
        # 规则 3: 内部重定向 → 安全
        # 规则 4: Debug 已关闭 → 不暴露
        # 规则 5: Git 文字提及无文件链接 → 误报
```

**Prompt 结构**:

```
SYSTEM_PROMPT（系统提示词 — 角色 + 判定标准）
  ├─ 三类结果的不同判定逻辑
  │   ├─ 技术识别类: 不是漏洞，不标误报
  │   ├─ 漏洞利用类: 三种误报场景（教程页/转义/无关文本）
  │   └─ 配置暴露类: 两种误报场景（通用错误页/已关闭声明）
  └─ 输出格式: JSON 数组

BATCH_PROMPT（用户提示词 — 具体数据）
  ├─ {count} 条待分析结果
  ├─ {items} 每条结果的摘要（URL + 名称 + 标签 + 请求/响应）
  └─ 输出恰好 {count} 个对象
```

**关键设计 — 响应摘要函数**:

```python
def _summarize_result(result, index):
    # 构建给 LLM 看的数据摘要
    # 重要：HTTP 响应分头部和正文两部分
    parts = response.split("\r\n\r\n", 1)
    headers = parts[0][:200]   # 头部截 200 字符
    body = parts[1][:600]      # 正文截 600 字符
    # URL 路径放在最前面 → LLM 优先获得路径上下文
```

**5 条硬规则详解**:

| # | 规则 | 触发条件 | 原理 |
|---|------|---------|------|
| 1 | 技术识别保护 | `finding_type == "tech"` | 技术栈识别不是漏洞，AI 可能误判 |
| 2 | XSS 转义检测 | body 含 `&lt;script&gt;` 且不含 `<script>` | 已做 HTML 实体编码 = 安全 |
| 3 | 内部重定向 | Location 头以 `/` 开头 | 内部跳转 ≠ 开放重定向 |
| 4 | 调试已关闭 | body 含 "Debug Mode: OFF" 或 "redacted" | 已主动关闭调试功能 |
| 5 | Git 文字提及 | 提及 "Index of /.git" 但无 `<a href>` 链接 | 文字描述 ≠ 真实文件列表 |

**Bug 修复记录**:

1. **响应预览 Bug**: `response.split("\r\n\r\n")[0]` → 只拿到 HTTP 头部
   - 修复: `parts[0][:200]` + `parts[1][:600]` 分别取头和体
2. **`.format()` 注入**: 响应体含 `{` 时 Python 的 `.format()` 抛 KeyError
   - 修复: 改用 f-string：`f"分析以下 {count} 条..."`

---

### 3.3 `core/fingerprint.py` — 指纹识别（119 行）

**职责**: 用 httpx 探测目标技术栈 → 匹配官方模板

```python
# 技术→模板映射表
TECH_TEMPLATE_MAP = {
    "apache":  ["http/technologies/apache/"],       # 目录 → 多个模板
    "php":     ["http/technologies/php-detect.yaml"], # 单个文件
    "flask":   ["http/technologies/flask-detect.yaml"],
    # ... 12 个常见技术栈
}

# 匹配逻辑（关键：token 化匹配）
tech_name = "Apache HTTP Server:2.4.57"    # httpx 原始输出
tech_name = tech.split(":")[0].lower()     # → "apache http server"
tokens = {"apache", "http", "server"}      # → 分词
for map_key in TECH_TEMPLATE_MAP:          # → "apache" in tokens ✓
```

**面试重点**: token 化是为了处理 httpx 输出的多单词技术名（如 "Apache HTTP Server"），比简单的 `in` 匹配更精确，避免了 "go" 误匹配 "mongodb" 的问题。

---

### 3.4 `core/report_generator.py` — 报告生成（160 行）

**职责**: LLM 生成安全摘要 + 结构化数据 + 优雅降级

```python
# 核心流程
generate_report_data(results, confirmed, fp):
    → severity_counts 统计（critical/high/medium/low/info）
    → 按严重度排序
    → 返回结构化 dict

generate_llm_summary(confirmed):
    → 构建 Prompt（漏洞列表 JSON）
    → 调用 Ollama
    → 解析 JSON：{overall_grade, severity_counts, top_issues, summary}
    → 失败则 fallback: 纯代码统计（无需 LLM）
```

**优雅降级设计**:
```python
def _fallback_summary(results):
    # 不需要 LLM 也能生成基本报告
    if critical > 0:  grade = "F"
    elif high > 0:    grade = "D"
    elif medium > 0:  grade = "C"
    # ...
```

---

### 3.5 `test-target/vuln_server.py` — 漏洞靶场（410 行）

**核心概念**: 用 Python 标准库 `http.server` 模拟一个有漏洞的 Web 应用

**设计原则**:
- 零外部依赖（只用 Python 标准库）
- 监听 `127.0.0.1`（仅本地可达）
- 所有漏洞均为静态 HTML，无真实危害
- 伪造 HTTP 响应头模拟 Apache+PHP 技术栈

**14 个真实漏洞**:

| # | 端点 | 类型 | 检测关键词 |
|---|------|------|-----------|
| 1 | `/search?q=<script>...` | XSS | `<script>alert` |
| 2 | `/redirect?url=evil.com` | 开放重定向 | `Location: evil.com` |
| 3 | `/debug` | 调试暴露 | `python_version`, `pid` |
| 4 | `/admin` | 后台暴露 | "Admin Panel" |
| 5 | `/.git/` | Git 泄露 | "Index of /.git" + `<a href>` |
| 6 | `/error` | 堆栈泄露 | "Traceback", "DatabaseError" |
| 7 | `/login` | 凭证泄露 | "test credentials", "admin123" |
| 8 | 响应头 | 版本泄露 | `Server: Apache/2.4.57` |
| 9 | `/api/users?id=1'` | SQLi | "SQLSTATE", "MySQL" |
| 10 | `/fetch?url=...` | SSRF | "URL Fetcher" |
| 11 | `/files/` | 目录浏览 | "Index of /files/" |
| 12 | `/api/data` | CORS | `Access-Control-Allow-Origin: *` |
| 13 | 全部页面 | 缺少安全头 | 无 X-Frame-Options |
| 14 | `/backup/.env.production` | 敏感文件 | "DB_PASS", "SECRET_KEY" |

**6 个误报干扰项**:

| # | 端点 | 触发模板 | 为什么是误报 |
|---|------|---------|-------------|
| 1 | `/blog/xss-tutorial` | XSS | 安全教程文章 |
| 2 | `/about` | Git 泄露 | 文字提及，无文件链接 |
| 3 | `/goto?url=...` | 开放重定向 | 始终跳转到内部路径 |
| 4 | `/safe-search?q=...` | XSS | 用户输入已 HTML 转义 |
| 5 | `/status` | 调试暴露 | 显示 "Debug Mode: OFF" |
| 6 | `/oops` | 堆栈泄露 | 通用错误页，无堆栈 |

---

### 3.6 `custom-templates/` — 20 个 Nuclei 模板

**模板分类**:

```
技法演示（5个）:
  01-simple-word.yaml          word 匹配 → 关键词检测
  02-header-body.yaml          header + body 分区匹配
  03-regex.yaml                regex 匹配 → 模式匹配
  04-dsl-complex.yaml          DSL 表达式 → 复杂条件
  05-multi-path.yaml           多路径 + stop-at-first-match

漏洞检测（15个）:
  vuln-xss-reflected.yaml      XSS 检测
  vuln-open-redirect.yaml      开放重定向（精确匹配）
  vuln-redirect-generic.yaml   开放重定向（通用匹配）
  vuln-debug-exposure.yaml     调试端点暴露
  vuln-git-exposure.yaml       .git 泄露
  vuln-admin-exposure.yaml     后台暴露
  vuln-credentials-comment.yaml 凭证泄露
  vuln-stacktrace-leak.yaml    堆栈泄露
  vuln-server-header.yaml      服务器版本泄露
  vuln-sqli-error.yaml         SQL 注入错误
  vuln-ssrf.yaml               SSRF
  vuln-directory-listing.yaml  目录浏览
  vuln-cors-misconfig.yaml     CORS 配置错误
  vuln-sensitive-file.yaml     敏感文件暴露
  vuln-missing-security-headers.yaml 缺少安全头
```

**模板结构（面试要能讲）**:

```yaml
id: 唯一标识符
info:
  name: 模板名称（中文或英文）
  severity: critical|high|medium|low|info
  tags: 标签（影响AI分类）
http:                       # ← 老协议格式（nuclei v2）
  - method: GET
    path:                   # 要请求的路径列表
      - "{{BaseURL}}/端点1"
      - "{{BaseURL}}/端点2"
    matchers:               # 匹配条件
      - type: word          # word|regex|status|dsl
        part: body          # body|header|all
        words:
          - "关键词1"
          - "关键词2"
        condition: and      # and|or
```

---

### 3.7 前端文件

**`web/templates/index.html`** (Dashboard):
- 深色主题（`#0f172a` 背景），原生 CSS
- 扫描表单 → 结果表格（带 AI 判定 + 置信度条）
- 指纹信息展示行（状态码、服务器、技术栈标签）
- 智能匹配模板数指示器
- 扫描对比选择器（2+ 条记录时显示）
- AI 汇总区：确认数 + 误报数

**`web/templates/report.html`** (报告):
- 打印优化（`@media print`、`@page`）
- 安全评级（A/B/C/D/F 彩色徽章）
- 严重度分布网格
- 漏洞详情表 + 误报表
- 一键打印 PDF 按钮

**`web/templates/compare.html`** (Diff 对比):
- 3 列摘要（Fixed / New / Persistent）
- 重选对比的下拉菜单
- 每类独立表格展示

---

## 4. 技术决策

### 决策 1: 为什么要批量分析而不是逐条？

| | 逐条 | 批量 |
|---|:--:|:--:|
| 耗时 | 5 分钟 | 1 分钟 |
| 准确率 | 低（无上下文） | 高（相对判断） |
| LLM 调用次数 | N 次 | 1 次 |
| 失败影响 | 单条失败可重试 | 整批失败需重来 |

**结论**: LLM 看到全部结果的上下文时，能更好地做相对判断（"哪些像真正的漏洞 vs 哪些像信息提示"）。

### 决策 2: 为什么用 8B 模型而不是 API？

| | 本地 8B | GPT-4 API |
|---|:--:|:--:|
| 费用 | 免费 | ~$0.03/次扫描 |
| 延迟 | 17-33s/批 | ~3s/批 |
| 数据隐私 | 不出本机 | 上传 OpenAI |
| 一致性 | 75-100% 波动 | 95%+ 稳定 |
| 部署难度 | 需要 5GB+ 显存 | 只需 API Key |

**结论**: 对于演示项目，零费用和数据隐私更重要。模型波动问题通过混合防线解决。架构上保留了切换 API 的能力。

### 决策 3: 为什么用 `http:` 而不是 `requests:` 格式？

Nuclei v3 支持两种模板格式：
- `http:` — 老格式，简洁（用于我们的自定义模板）
- `requests:` — 新格式，支持多协议组合（更复杂）

我们的模板只用 HTTP 协议，用老格式更简洁。注意 Nuclei v3.8 开始对老格式发 warn，但不影响使用。

### 决策 4: 为什么 BATCH_SIZE=5？

平衡点——对于 8B 量化模型：
- 太少（1-2）：浪费时间在多轮推理上
- 太多（10+）：模型不稳定，容易截断 JSON，准确率下降
- 5：单次推理 ~25s，JSON 解析稳定，AI 能充分关注每条结果

### 决策 5: 硬规则为什么是 5 条而不是更多？

硬规则的取舍：
- **确定性场景**才加规则（如 HTML 实体编码 = 绝对安全）
- **模糊场景**留给 AI（如教程页判断 = 语义理解）
- 规则太多 → 维护成本高，规则之间可能冲突
- 5 条刚好覆盖当前靶场的所有确定性误报场景

---

## 5. 模板 DSL

### Matcher 类型速查

| 类型 | 匹配对象 | 示例 | 适用场景 |
|------|---------|------|---------|
| `word` | 响应中的关键词 | `words: ["admin"]` | 页面内容检测 |
| `regex` | 正则表达式 | `regex: ["DB_(PASS\|USER)"]` | 模式匹配 |
| `status` | HTTP 状态码 | `status: [200, 301]` | 响应状态检测 |
| `dsl` | DSL 表达式 | `dsl: ["len(body) < 100"]` | 复杂条件组合 |

### 常用 DSL 函数

```yaml
# 检查响应头是否缺失
!contains(to_lower(header), 'x-frame-options')

# 检查状态码范围
status_code >= 200 && status_code < 300

# 检查正文长度
len(body) > 0

# 正则匹配
contains(body, 'admin')

# 判断响应是否包含某字符串
contains(to_lower(header), 'nginx')
```

---

## 6. 面试叙事

### 30 秒电梯演讲

> "我做了一个 AI 增强的漏洞管理平台。核心问题是：自动化扫描器误报率太高。我的方案是在 Nuclei 扫描引擎之上加一层 AI 分析——用本地大模型批量分析扫描结果，再结合 5 条硬规则修正，准确率做到 90% 以上。项目用了 Python FastAPI + Ollama，写了 20 个检测模板和含干扰项的漏洞靶场来验证效果。"

### 2 分钟详细版（面试标准回答）

> **问题**: "安全扫描器的最大痛点是误报。Nuclei 是优秀的扫描引擎，但它只做规则匹配不做分析——它告诉你匹配到了关键词，但不关心这是真实漏洞还是教学代码。安全团队每天要花 30% 时间人工审核扫描结果。"
>
> **方案**: "我的方案是混合防线——AI 做语义理解，硬规则做确定性修正。AI 层用 Ollama 本地跑 qwen3:8b，批量分析扫描结果。关键优化是 Prompt 工程——我经过 5 轮迭代发现，场景化指引比抽象规则有效得多。比如告诉模型'标题含教程的页面通常是教学文章'，比'区分教育内容和漏洞'更有效。"
>
> **难点**: "遇到两个关键问题。第一，8B 量化模型不够稳定，同一数据多次测试准确率在 75% 到 100% 波动。所以我加了 5 条硬规则——比如'响应中 script 标签已被 HTML 实体编码就自动标误报'，这些确定性场景不需要 AI 判断。第二，排查了两天才发现 AI 判定质量差是因为响应摘要函数取的是 HTTP 头而不是正文——AI 一直在盲猜。"
>
> **验证**: "为了验证效果，我自建了含干扰项的漏洞靶场——14 个真实漏洞加 6 个误报干扰。写了 20 个 Nuclei 模板覆盖 OWASP Top 10。最终测试 AI 加硬规则综合准确率做到 100%。"

---

## 7. 追问清单

### Q: 为什么不用 Docker？

"Docker Desktop 在 Windows 11 Home China 环境启动失败，而且 Docker Hub 被墙。塞翁失马——自建靶场反而更有面试价值，因为它展示了'靶场设计能力'和'扫描检测能力'两个维度。"

### Q: AI 不可用时怎么办？

"架构支持优雅降级。如果 Ollama 连接失败，`filter_results()` 会返回空判定的默认结果（类型 unknown、非误报）。扫描仍然可用，只是没有 AI 判定。报告生成也有 fallback——纯代码统计生成基本摘要。"

### Q: 怎么扩展到生产环境？

"三个方向：第一，SCAN_HISTORY 从内存改数据库（SQLite/PostgreSQL）；第二，接入更大模型（qwen3:32b 或云端 API）提升稳定性；第三，支持 Nuclei 官方全量模板库，按指纹智能选择。"

### Q: 模板的 FP 干扰项怎么设计的？

"核心原理：页面 HTML 中保留模板匹配的字面量关键词，让模板先触发，再由 AI 来判定是真漏洞还是误报。比如教程页在 `<pre><code>` 中放未转义的 `<script>` 标签——Nuclei 匹配到关键词触发告警，AI 看到标题'XSS 入门教程'和代码块结构，判定为教学文章。"

### Q: 你怎么知道 AI 判得对？

"对照实验。先确认每个端点应该被判定为什么——漏洞列表是预先设计的。然后跑扫描，对比 AI 判定与预期。多轮测试记录波动性。8B 模型的一致性问题是重要的发现——直接导致了混合防线的设计。"

---

## 附录: 项目启动命令速查

```bash
# 启动 Ollama
ollama serve

# 启动漏洞靶场（端口 9999）
cd test-target && python vuln_server.py 9999

# 启动 Web 面板（端口 8000）
python -m uvicorn web.app:app --host 127.0.0.1 --port 8000

# 单独测试 Nuclei 模板
nuclei -target http://127.0.0.1:9999 -t custom-templates/ -silent

# 单独测试指纹识别
python -c "from core.fingerprint import fingerprint_target; print(fingerprint_target('http://127.0.0.1:9999'))"
```
